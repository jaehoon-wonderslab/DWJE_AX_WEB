import './_rnw';
import React from 'react';
import { LoginScreen } from 'dwje-ax-web';

/** 로그인 — /login (데모 계정 · 목 데이터) */
export const Default = () => (
  <div style={{ width: 1280, height: 800 }}>
    <LoginScreen />
  </div>
);
