import './_rnw';
import React from 'react';
import { ScreenQcAoi } from 'dwje-ax-web';

/** AOI 판정 분석·예측 — /quality/aoi (데모 계정 · 목 데이터) */
export const Default = () => (
  <div style={{ width: 1280, height: 800 }}>
    <ScreenQcAoi />
  </div>
);
