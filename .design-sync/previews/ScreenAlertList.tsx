import './_rnw';
import React from 'react';
import { ScreenAlertList } from 'dwje-ax-web';

/** 알림 목록·상세 — /alert/list (데모 계정 · 목 데이터) */
export const Default = () => (
  <div style={{ width: 1280, height: 800 }}>
    <ScreenAlertList />
  </div>
);
