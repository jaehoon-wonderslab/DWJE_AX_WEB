import './_rnw';
import React from 'react';
import { ScreenMenuReport } from 'dwje-ax-web';

/** 보고서 허브 — /menu/report (데모 계정 · 목 데이터) */
export const Default = () => (
  <div style={{ width: 1280, height: 800 }}>
    <ScreenMenuReport />
  </div>
);
