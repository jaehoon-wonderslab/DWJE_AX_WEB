import './_rnw';
import React from 'react';
import { ScreenSysRank } from 'dwje-ax-web';

/** 제품군 순위 관리 — /system/product-rank (데모 계정 · 목 데이터) */
export const Default = () => (
  <div style={{ width: 1280, height: 800 }}>
    <ScreenSysRank />
  </div>
);
