import './_rnw';
import React from 'react';
import { ScreenRptScrap } from 'dwje-ax-web';

/** 폐기 보고서 — /report/scrap (데모 계정 · 목 데이터) */
export const Default = () => (
  <div style={{ width: 1280, height: 800 }}>
    <ScreenRptScrap />
  </div>
);
