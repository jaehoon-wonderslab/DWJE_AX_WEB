import './_rnw';
import React from 'react';
import { SignupScreen } from 'dwje-ax-web';

/** 회원가입 — /signup (데모 계정 · 목 데이터) */
export const Default = () => (
  <div style={{ width: 1280, height: 800 }}>
    <SignupScreen />
  </div>
);
